# Shared Crate

Dependency-light contracts shared by the runtime and console:

- `types` — lifecycle and status values
- `errors` — public error types
- `events` — lifecycle events
- `config` — shared runtime configuration

